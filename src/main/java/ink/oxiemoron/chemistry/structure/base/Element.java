package ink.oxiemoron.chemistry.structure.base;

public interface Element {
    // still playing around with this being abclass/interface
    // but it's really just a placeholder
    // plan right now:

    // Enter IUPAC name, get chem structure (choose your own type of thing, hah)
    // will actually try/start implementing the "simpler" structures

    // and then

    // we shall see bash


}
